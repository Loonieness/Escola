const { resolve } = require('path');
module.exports = {
  config: resolve(__dirname, 'src', 'config', 'database.js'),//aqui fica o arquivo de configuração
  'models-path': resolve(__dirname, 'src', 'models'),//o caminho dos models
  'migrations-path': resolve(__dirname, 'src', 'database', 'migrations'),//migrations serão alterações na tabela, uma timeline
  'seeders-path': resolve(__dirname, 'src', 'database', 'seeds'),
};
